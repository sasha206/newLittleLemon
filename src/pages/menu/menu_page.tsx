import styled from 'styled-components';

// Styled Components
const MenuContainer = styled.div`
    text-align: center;
`;
// Компонент меню
const Menu = () => {

    return (
        <MenuContainer>
            <h1>Our Menu</h1>
        </MenuContainer>
    );
};

export default Menu;
